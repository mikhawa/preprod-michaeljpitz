{% extends 'base.html.twig' %}

{% block title %}Accès interdit - Portfolio{% endblock %}

{% block body %}
    <div class="flex flex-col items-center justify-center py-20 text-center">
        <div class="text-8xl mb-6">🚫</div>
        <h1 class="text-4xl font-bold text-[var(--accent)] mb-4">403</h1>
        <p class="text-2xl font-semibold text-[var(--text-primary)] mb-2">Accès interdit pour toi...</p>
        <p class="text-[var(--text-secondary)] mb-8">Tu n'as pas les droits nécessaires pour accéder à cette page.</p>
        <a href="{{ path('app_home') }}"
           class="inline-block px-6 py-3 bg-[var(--accent)] text-white rounded-lg hover:bg-[var(--accent-hover)] transition-colors font-medium">
            Retour à l'accueil
        </a>
    </div>
{% endblock %}
